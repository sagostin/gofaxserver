<include>

  <!-- REQUIRED: set sofia_ip to this host's LAN IP. It is used for the SIP
       and RTP bind addresses AND the external (advertised) IP, so a wrong
       value breaks both signaling and media. See docs/INSTALLATION.md. -->
  <X-PRE-PROCESS cmd="set" data="sofia_ip=172.16.25.30"/>

</include>
